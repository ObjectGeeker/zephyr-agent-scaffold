# 基础配置
spring:
  application:
    name: zephyr-agent-scaffold-app
  # 引入Agent配置；.env 存放密钥等敏感配置（已被 .gitignore 排除）
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:../.env[.properties]
      - classpath:agents/single-agent.yml
server:
  servlet:
    context-path: /api
  port: 8123